name: Generate Snake Animation

on:
  schedule:
    - cron: "0 0 * * *" # Runs daily at midnight UTC
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Generate Snake SVG (Dark Grid + Green Contributions)
        uses: Platane/snk@v3
        with:
          github_user_name: jakebasa
          outputs: dist/github-contribution-grid-snake.svg?palette=github-dark&color_snake=#3CEB9A&color_dots=#00FF99,#32CD32,#228B22,#145A32

      - name: Push Snake Animation to Output Branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ github.token }}
